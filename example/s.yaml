edition: 1.0.0
name: component-test
# access: default # 密钥别名

services:
  component-test:
    component: ${path(..)}
    props:
      codeUri: ../dist
      serviceName: build-link
      functionName: test
      # excludeFiles:
      #   - common
